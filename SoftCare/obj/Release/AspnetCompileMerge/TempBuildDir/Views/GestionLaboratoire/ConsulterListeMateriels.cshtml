@model IEnumerable<SoftCare.Models.PRODUIT>

@{
    Layout = "../../Views/Shared/_SoftCareLayoutPage.cshtml";
    ViewBag.Title = "Liste Materiels du Laboratoire Médical";

    ViewBag.Btndisabled = "disabled=\"disabled\";";
}

<div class="row" style="margin:20px 0">

    <div class="col-lg-5">
        <a href="" class="btn btn-primary">
            <span class="glyphicon glyphicon-plus"></span>
            Ajouter un Materiels au Laboratoire Médical
        </a>
    </div>

</div>


<div class="row">

    <section class="col-lg-12 table-responsive" style="max-height: 37em;overflow-x: auto;">
       
            <table class="table table-striped table-condensed" style="margin-bottom:0px;">

                @if (Model == null)
                {
                    <caption style="color:#000"><h6> Aucun Matériel ou service enregistré </h6></caption>
                }
                else
                {
                    <thead>
                        <tr>
                            <th>Réf. Produit</th>
                            <th>Intitulé</th>
                            <th>Fournisseur</th>
                            <th>Quantité Stock</th>
                        </tr>
                    </thead>
                    <tbody>
                        
                        @foreach (var Produit in Model)
                        {
                            if (Produit != null)
                            {                                
                                <tr>
                                    <td><a href=""> @Produit.Reference </a></td>
                                    <td>@Produit.Nom</td>
                                    <td>@Produit.Fabricant.Nom</td>
                                    <td>@Produit.SeuilAlert</td>
                                </tr>
                            }
                        }
                    </tbody>
                }
            </table>
    </section>

</div>